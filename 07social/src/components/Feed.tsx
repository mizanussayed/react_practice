import { Box } from "@mui/material"
import { yellow } from "@mui/material/colors"


const Feed = () => {
    return (
        <Box flex={4} p={2} bgcolor={yellow}>
            Feed
        </Box>
    )
}

export default Feed